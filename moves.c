void draw_three(deck *draw, deck *reference)
{
	
}

void reverse(deck *target)
{
	deck *ans = { .type = target->type, .first = NULL, .next = NULL };
	card *current = target->first;
	while (current->next != NULL)
	{
		
	}
}
